{
  "name": "sampleapi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clear": "del .aws-sam && del dist",
    "build": "tsc",
    "watch": "tsc -w",
    "deploy": "npm run clear && tsc && sam build && sam deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-sdk": "^2.7.0",
    "@types/jsonwebtoken": "^9.0.1",
    "@types/node": "^18.15.0",
    "del-cli": "^5.0.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "esbuild": "^0.17.11",
    "jsonwebtoken": "^9.0.0"
  }
}
